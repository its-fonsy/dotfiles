# dotfiles

This repo is the home of my dotfiles.

## Install

Run

	git clone --recursive https://github.com/its-fonsy/dotfiles.git
	make init

## Configuration

The program that I use are:

- Terminal: `foot` 
- Shell: `zsh`
- Editor: `neovim`
- DE: `sway`
- Bar: `swaybar` 

